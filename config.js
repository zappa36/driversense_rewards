'use strict';

/* ============================================================
 * Supabase configuration.
 *
 * Fill these in to connect the driver app and Challenge Studio
 * to a shared Supabase backend (see README "Connecting to
 * Supabase"). Both values come from your Supabase project:
 * Project Settings -> API. The anon key is designed to be
 * public — access is controlled by Row Level Security.
 *
 * Leave both empty to run in local demo mode (hardcoded data,
 * planner code gate).
 * ============================================================ */

window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
